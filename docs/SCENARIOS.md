# Demo Scenarios

Some ready-made scenarios you can run or extend.

---

## Scenario 1 — Basic Run

- Simulate 5 timesteps, 5 devices
- Print:
  - First 5 rows
  - All 5G routes with `latency_ms < 40`

CLI:

```bash
python -m law_n_sql_playground.cli
